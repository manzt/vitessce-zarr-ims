## Development

Serve data

```bash
cd data
python csv2zarr.py
python -m http.server # data servered on localhost:8000
```

React app

```bash
yarn install
yarn start # navigate to localhost 3000
```
